/*
*
* @ With css solved print problem.
    It will print just printed area which id is selected and other all element id class will hide when command to print.
*
*/

/* print css */
@media print {
  body * {
    visibility: hidden;
  }
  #root, #root * {
    visibility: visible;
  }/*hide all element from print*/
  #root {
    position: absolute;
    left: 0;
    top: 0;
    border: none;
  } /*show only printable id for fint*/
}
